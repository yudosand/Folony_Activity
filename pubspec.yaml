name: folony_activity
description: Scaffold awal aplikasi mobile HEX Activity.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^1.2.1
  camera: ^0.12.0+1
  google_mlkit_face_detection: ^0.13.2
  geolocator: ^14.0.2
  image: ^4.5.4
  shared_preferences: ^2.5.3
  firebase_core: ^4.1.1
  firebase_messaging: ^16.0.2
  flutter_local_notifications: ^19.4.0
  timezone: ^0.10.1
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
